name: gfx_tool_mvismad
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  file_picker: ^5.3.2
  flutter:
    sdk: flutter
  path_provider: ^2.0.15
  permission_handler: ^10.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
